def add_certifications(current, new):
    return current + new


print(f"Total Certifications: {add_certifications(5, 3)}")

# This code defines a function to add the number of certifications and prints the total.
